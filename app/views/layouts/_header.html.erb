<div id="wrapper">
  <div id="menu-wrapper">
    <div id="menu">
      <ul>
        <li class="current_page_item"><%= link_to "[ Y ]", root_path %></li>
        <% if logged_in? %>
          <li><%= link_to "Create Post", new_post_path %></li>
          <li><%= link_to "Logout", logout_path, method: :delete, id: "logout-link" %></li>
        <% else %>
          <li><%= link_to "login", '/login' %></li>
          <li><%= link_to "register", '/users/new' %></li>
        <% end %>
      </ul>
    </div>
  </div>

  <div id="header-wrapper">
    <% if logged_in? %>
      <h4> Welcome <%= current_user.name %> to Hacker(ish) News!</h4>
    <% else %>
      <h4>Welcome to Hacker(ish) News</h4>
    <% end %>
  </div>
</div>
<div class="container">
